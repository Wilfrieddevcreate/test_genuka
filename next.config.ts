import type { NextConfig } from "next";

const nextConfig: NextConfig = {

  images: {
    domains: ['i.pravatar.cc'], // Ajoute 'i.pravatar.cc' dans la liste des domaines autorisés
  },

};

export default nextConfig;
